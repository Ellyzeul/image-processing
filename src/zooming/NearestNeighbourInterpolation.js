/**
 * @function
 * 
 * @param {ImageData} imageData
 * @param {number} scaleResize
 */
const NearestNeighbourInterpolation = (imageData, scaleResize) => {
  const data = imageData.data
}

export default NearestNeighbourInterpolation
